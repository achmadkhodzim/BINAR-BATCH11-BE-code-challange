<%= form_with(model: @book, local: true) do |form| %>

  <% if @book.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@book.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @book.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= form.label :nama %><br>
    <%= form.text_field :nama %>
  </p>

  <p>
    <%= form.label :penerbit %><br>
    <%= form.text_field :penerbit %>
  </p>
  <p>
    <%= form.label :stok %><br>
    <%= form.number_field :stok %>
  </p>
  <p>
  <%= form.select(:category_id, options_from_collection_for_select(Category.all, :id, :nama)) %>

  </p>
  <p>
    <%= form.submit %>
  </p>

<% end %>
